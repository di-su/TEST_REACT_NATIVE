import * as React from 'react';
import {ScrollView, Text} from 'react-native';
import styles from '../../resources/styles/styles';

function AddLeads() {
  return (
    <ScrollView>
      <Text style={styles.bigText}>Add Leads</Text>
    </ScrollView>
  );
}

export default AddLeads;
